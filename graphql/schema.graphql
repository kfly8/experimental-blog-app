type Query {
    greatEntry: GreateEntryPayload
}

type GreateEntryPayload {
    point: String!
    entry: Entry!
}

type Entry {
    id: ID!
    title: String!
    body: String!
    comments: [EntryComment!]
}

type EntryComment {
   id: ID!
   body: String!
}

